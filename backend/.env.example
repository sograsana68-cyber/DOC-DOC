# MongoDB Atlas connection string (SRV URI from Atlas → Connect → Drivers)
MONGODB_URI=mongodb+srv://<user>:<password>@<cluster>.mongodb.net/<dbname>?retryWrites=true&w=majority

# Optional HTTP port
# PORT=3000
