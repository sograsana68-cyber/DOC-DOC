/**
 * Placeholder for vault/document APIs.
 */

export function listDocumentsPlaceholder(_req, res) {
  res.status(501).json({
    error: "Not implemented",
    hint: "Add Document model and CRUD routes when ready.",
  });
}
